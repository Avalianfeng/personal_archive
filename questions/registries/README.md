# registries/ · 规则层

> ⛔ **编辑请只改 YAML。** 查看标签/prerequisite 请用：
>
> ```bash
> python questions/qcli.py registry list tags
> python questions/qcli.py registry list prerequisites
> ```

| 文件 | 说明 |
| --- | --- |
| `tags.yaml` | 主题 tag 标准库 |
| `prerequisites.yaml` | 语义严格前提 |

人类可读快照（可选）：`generated/registries_human_readable.md`（export 自动生成）
