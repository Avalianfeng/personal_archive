# 题目格式规范 · v1.0

> **状态**：DB 时代 · 2026-06-16  
> **关联**：[question.schema.json](./question.schema.json) · [整理规范-v1.0.md](../整理规范-v1.0.md)

---

## 架构

```text
question_registry/questions.db   题目存储真源
registries/*.yaml                tags / prerequisites 语义真源
        ↓ sync_categories.py
categories/*.md                    导航视图（禁止手改）
        ↓ export_json.py
generated/questions.json | stats.json
```

日常编辑：`python questions/qcli.py edit …`  
Agent 导入：`imports/pending/*.jsonl` → `ingest.py`

---

## Agent 导入契约（jsonl）

每行一个 JSON 对象。完整字段表见 [整理规范-v1.0.md §jsonl 字段契约](../整理规范-v1.0.md#jsonl-字段契约)。

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `question` | 是 | 题干 |
| `category` | 是 | slug，与 `categories/*.md` 对应 |
| `subcategory` | 是 | sync 写入 `##` 标题 |
| `type` | 是 | 见下表 |
| `source` | 建议 | 来源名 |
| `interaction` | 建议 | 回答交互方式 |
| `options` | 条件 | `agreement` / `single` / `multi` 必填 |
| `scale_note` | 否 | `type: scale` 时可选 |
| `tags` / `prerequisites` | 否 | 须登记于 registries yaml |

Agent **不提供** `uid` / `id` — ingest 自动生成。

### type · options · scale_note

| type | 含义 | options | scale_note |
| --- | --- | --- | --- |
| `open` | 开放叙述 | — | — |
| `fill` | 填空（题干含 `_____`） | — | — |
| `agreement` | 认同度 / 「像我」 | **必填** `[{key,text},…]` | — |
| `single` / `multi` | 单选 / 多选 | **必填** | — |
| `scale` | 数值量表 | 可选 | 常用 |
| `sort` | 排序 | 视题型 | — |

### options 格式（与 schema 一致）

```json
"options": [
  {"key": "1", "text": "完全不像我"},
  {"key": "2", "text": "不太像我"}
]
```

- `key`：展示用序号/字母（`"1"`…`"6"` 或 `"A"`…）
- `text`：选项文案
- **禁止** `["完全不像我", "不太像我"]` 字符串数组 — ingest 拒绝，sync 无法渲染

`ingest.py` 入库前校验 options 形状；详见 `db.validate_options`。

---

## uid 与 id

| 字段 | 角色 |
| --- | --- |
| `uid` | **主键**（8 位 hex）；`related` 引用 uid |
| `id` | 人类编号 `Q-VAL-001`；**删题不重排** |
| `order` | 可选展示顺序 |

---

## 文件与目录（V2）

```text
source_library/              来源卡片 + attachments/
  attachments/pending/       待整理全文
  attachments/processed/     已整理对照全文
imports/pending/             Agent jsonl 收件箱
imports/processed/           ingest 归档
imports/external/            测评脚本（参考，不 bulk 入库）
rejected/                    来源/题目淘汰记录
```

详见 [source_library/README.md](../source_library/README.md)。

---

## 两层生命周期

| 维度 | 载体 |
| --- | --- |
| **文件** | attachments · imports · rejected/ |
| **题目** | DB `status`: active · candidate · deprecated |

---

## 系统不可提问（system_unaskable）

题干预设**在场对话者**（采访者↔被采访者）。整理阶段**不入库**，写入 [`rejected/system_unaskable.md`](../rejected/system_unaskable.md)。

---

## 字段参考（DB / sync 视图）

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `category` | 是 | slug |
| `subcategory` | 是 | |
| `type` | 是 | 见上表 |
| `status` | 是 | active · candidate · deprecated |
| `prerequisites` | 否 | 语义严格前提；见 [prerequisites.yaml](../registries/prerequisites.yaml) |
| `interaction` | 建议 | story · rating · reflection 等 |
| `source` | 建议 | |
| `tags` | 否 | 见 [tags.yaml](../registries/tags.yaml) |
| `related` | 否 | 题号或 uid |
| `validation` | 否 | 诚实度校验 |

**禁止**：difficulty · mapsTo · dimension · trait · guide

### prerequisites vs tags

| | prerequisites | tags |
| --- | --- | --- |
| 含义 | **无此事实则问题不存在** | 题面涉及什么主题 |
| 严格度 | 极高；绝大多数题省略 | 可逐步补充 |
| 例子 | `has_current_partner` | `relationship` |

宗教 / 政治：**不新开一级分类**；按功能入现有 category，用 `tags` 检索。

---

## 边界（问题库 vs 引擎）

| 层 | 放什么 | 本阶段 |
| --- | --- | --- |
| **questions/** | 问什么、能否成立 | ✅ |
| **registries/** | 标准 id | ✅ |
| **question_guides/** | 怎么问 | ❌ 暂不做 |
| **analysis_guides/** | 按题预置解读 | ❌ 暂不做 |

---

## 健康检查

```bash
python questions/scripts/manage.py sync    # DB → categories + export
python questions/qcli.py doctor          # 区分 STALE（未 sync）与 DATA ERROR
```

`doctor` 报 `STALE` → 运行 `manage sync`；报 `ERROR: Q-xxx: options` → 修正 DB 或 jsonl 重导。
